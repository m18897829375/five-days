【睿迄科技】全栈开发5天挑战
全栈挑战 · 后端架构与核心业务逻辑
在你开始之前
我们想看的不是你能不能"跑通"一个表单，而是——你能不能把一个看似简单的健康测评，从数据库到接口到鉴权，搭出一个站得住的工程骨架。
API 设计的合理性、数据建模的稳健性、流程的闭环、以及你和 AI 的协作效率，是我们最关心的四件事。
UI 不在考查范围内，能跑就行。

一、参考竞品
请先体验一下这个产品，了解一下完整的测评流程和数据结构，再开始动手——你将要搭建的后端，就是支撑这种产品的核心基础设施：
🔗 BetterMe Quiz Funnel：https://betterme-pilates.com/first-page-brand-palette?flow=2117
我们不要求你 1:1 复刻它的功能，但希望你认真观察它的数据流——哪些步骤产生哪些数据、哪些数据需要持久化、订阅前后用户能拿到什么——并在你的设计里做出你认为更合理的判断。

二、任务目标
设计并开发一个健康测评系统的核心后端架构。我们看重的是：
API 设计规范：路径、方法、请求 / 响应结构是否专业
数据库建模稳定性：表结构是否可扩展、字段选择是否合理
数据持久化逻辑：分步保存、进度恢复、状态一致性
模拟订阅体系：鉴权、差异化返回、支付回调闭环

三、核心流程（后端导向）
第一阶段 · 测评数据流与状态恢复（Persistence）
功能点：用户填写性别、目标、身体数据（年龄、身高、体重、目标体重）及运动频率。
技术要求：
分步保存接口：用户每完成一步，前端调用接口将增量数据同步至后端
进度恢复逻辑：用户中途关闭页面再次进入时，后端需返回该用户已填写的进度数据（可基于随机生成的 UserID 或简易 Session 识别）
第二阶段 · 服务端计算逻辑（Core Logic）
功能点：用户提交所有数据后，触发后端逻辑处理。
技术要求：
在服务器端实现一套简单的"健康评估算法"，计算出 BMI、建议摄入量、目标预测日期
将计算结果持久化存储，并关联至用户记录
第三阶段 · 订阅鉴权与权限保护（Auth & Access）
功能点：结果页展示。
技术要求：
逻辑拦截：结果页接口需校验用户的 subscription_status（模拟状态）
差异化返回： 非会员：API 仅返回部分脱敏数据（如隐藏具体的预测曲线数据），并提示需付费
¡会员：API 返回完整数据
模拟支付回调：提供一个 /pay 接口，调用后修改数据库中的会员状态为有效

四、技术要求
● 前端：Next.js（App Router）或任何你擅长的框架。
  我们不评判动效细节和像素级还原，但前端要让真实用户愿意一路填到付费弹窗
  ——基础视觉、文案、节奏、信任感不能糊弄，以吸引用户的角度考量
● 后端（Core）：Node.js（Next.js API Routes / NestJS / Express 任选） + TypeScript
● 数据库：Supabase / Prisma + PostgreSQL
● 部署：必须提供一个公网可达、可完整演示的线上链接
  （我们会直接在线上跑一遍 funnel + 模拟支付的全流程）

五、交付物
1.完整可演示的线上链接（必填）    
a.公网可达的 URL，我们点进去能从头到尾走一遍 funnel    
b.含 /pay 接口的可重放调用方式（README 里给一段 cURL 或 Postman collection）    
c.提供一个已支付的测试 sessionId，让我们能直接对比付费前后的差异化返回
2.代码仓库：GitHub 链接，附带 README 说明如何启动及 API 文档
3.数据库 Schema 图：展示用户表、数据记录表、订阅信息表之间的关系
4.AI 使用复盘：简述你如何利用 AI 进行数据库建模、生成 Mock 数据、编写复杂逻辑
注意：前端要以吸引用户的角度考量

六、评分标准
后端功底 API 路径设计是否合理？数据验证是否严谨（能否防止非法数值注入、边界值是否处理到位）？
DB 设计 表结构是否具备扩展性？字段选择是否专业？关系建模是否经得起推敲？
逻辑闭环 从录入 → 存储 → 权限校验 → 模拟支付，整个流程是否能完整跑通？刷新、中断、异常路径是否都考虑到了？
AI 效率 你能否调教 AI 生成高质量的类型定义（Types）和数据库迁移脚本？是不是只把 AI 当代码生成器，还是真的在和它协作？
    
交付方式
5 天内将交付物发送至 yitengruntu12123@gmail.com，alex@arkon-tech.com， rip@arkon-tech.com 三个邮箱。
文档命名格式：【姓名】_全栈挑战_YYYYMMDD

期待看到你的作品。后续的面试会以该结果为准，决定是否继续推进面试
表现卓越者【甚至可以直接进入终面录取】+ 更大的薪资回报，期待您的答案～
