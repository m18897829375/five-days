{
  "mcpServers": {
    "playwright": {
      "type": "http",
      "url": "http://localhost:8931/mcp",
      "description": "Playwright MCP server for browser automation testing. HTTP transport avoids MSYS2 stdio pipe deadlock with large screenshots.",
      "env": {}
    },
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp"],
      "description": "Context7 MCP server for fetching up-to-date library documentation. Used by Generator when researching best practices before drafting contracts.",
      "env": {}
    }
  }
}
