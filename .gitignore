# Ralph runtime files (generated during runs)
.ralph/

# Archive is optional to commit
# archive/

# Environment variables (contains secrets)
.env

# Dependencies
node_modules/

# Next.js build output
.next/

# TypeScript incremental build info
tsconfig.tsbuildinfo

# Test artifacts (cookies, headers, screenshots)
cookie.txt
headers.txt
incomplete-cookie.txt
premium-cookie.txt
screenshot-home.png
test-results/

# OS files
.DS_Store

# Claude Code runtime files (keep examples)
.claude/*
!/.claude/settings.json.example
# NEVER commit local settings (contains API keys)
.claude/settings.local.json

.vercel
